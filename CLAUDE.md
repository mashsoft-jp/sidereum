# Sidereum — 作業上の約束

配信物は依存ライブラリなしの `index.html` 1枚と、天体テクスチャ `tex/*.jpg`、それに Service Worker の `sw.js`。バックエンドは導入せず「いけるところまで」進める方針。

テクスチャを HTML に埋め込まないのは、コードを1行直すたびに全員に画像ぜんぶを引き直させないため。代わりに `file://` では動かない (不透明オリジンのため `texImage2D` が SecurityError で弾かれ、天体が仮色になる) — HTTP で配信する前提。

## index.html は生成物

直接編集しない。`src/` を編集してビルドし直す。

```
node tools/build.mjs           # src/ → index.html
node tools/build.mjs --check   # index.html が src/ と一致するか検証 (CI でも実行)
```

生成物 `index.html` もリポジトリに含めるので、**コミットには `src/` の変更と `index.html` の両方を入れる**。`--check` が通らない状態で push しない。

## `src/**/*.js` はモジュールではない

同一スコープを共有するソース断片で、連結順そのものが意味を持つ (順序は `tools/build.mjs` の `MANIFEST` で固定)。並べ替えると壊れる。

個々の断片に構文チェック・型チェックをかけない。`src/gl/setup.js` は WebGL 初期化失敗時の早期脱出にトップレベル `return` を使っており、単独では構文的に不正。構文検査はビルドが連結後に行う。詳しくは README の「注意: これはモジュール分割ではありません」。

## 動作確認

```
python3 -m http.server 8934
```

`http://localhost:8934/index.html?v=N` を開く (`v` はキャッシュ回避用の任意の値)。描画に触れた変更は宇宙・地上・月面の3ビューを見る。再現用の URL 例:

```
?d=1910-05-20T06:00&sel=halley&play=0     # ハレー彗星のコマ・尾
```

描画が重いかを見るときは `?perf=1` を付ける (開発用。付けないときは計測も描画もしない)。フレーム間隔の棒グラフと、CPU 側の区間ごとの時間・描いた数・DPR が出る。**測れるのは CPU の時間だけ** — `gl.*` はコマンドを積むだけなので、区間の合計とフレーム間隔が大きく違うときは GPU 律速か 60fps の頭打ち。切り分けはメニューの切替 (星雲・星団 / 星座 / 光の滲み / 高解像度テクスチャ) を切って差を見る。実機は GitHub Pages に `?perf=1` を付けて開けばよい。

実機 (GitHub Pages) は `max-age=600` でキャッシュされる。**ハンバーガーメニュー末尾の `build YYYY/MM/DD HH:MM` が最新ビルドか確認してから**、直った・直っていないを判断する。

## 画像のキャッシュ (`sw.js`)

`tex/` は 2K 一組で約 7MB、4K で約 30MB ある。開くたびに引き直させないよう、Service Worker で Cache API に控える。

- **`tex/` とアイコン**: キャッシュ優先。一度取ったらネットワークへ出ない
- **それ以外 (`index.html`・manifest)**: ネットワーク優先。取れたら控えを更新し、オフラインのときだけ控えを使う

**`index.html` をキャッシュ優先にしてはいけない。** 配信した修正が届かなくなり、`build` 時刻での確認が成り立たなくなる。この境目は `tools/sw-test.mjs` で押さえてある。

```
node tools/sw-test.mjs   # 経路の切り分けとキャッシュの出し入れを確かめる
```

**`tex/` の画像を差し替えたら `sw.js` の `TEX_VER` を上げること。** ファイル名は固定なので、上げないとキャッシュを持っている人には新しい画像が永久に届かない。上げると古い組を丸ごと捨てるので、差し替えた1枚だけでなく全部を引き直すことになる (画像の差し替えは滅多に無いので、この単純さを採る)。

控える一覧は `sw.js` には書かず、ページ側 (`data/textures.js` の `texAllURLs`) から `postMessage` で渡す。二重管理を避けるため、また解像度の切替にそのまま追従させるため。渡すのは全部読み終わってから (`gl/resources.js` の `texPending` が 0 になったとき) — 読み込み中に渡すと、同じものをもう一度ネットワークから取りに行かせることになる。

Service Worker は **`http://localhost` か HTTPS でしか動かない**。Claude Code のブラウザペインでは登録自体ができないので、実際の動きを見るには実ブラウザの DevTools → Application → Service Workers / Cache Storage を使う。

## 描画コードを触るとき

WebGL の状態 (uniform・blend・depth・cull) はグローバルなので、経路ごとに暗黙で引き継がせない。設定漏れがあると直前の描画の値がそのまま残る (実際に `uComet` の設定漏れで、地上ビューの全天体が彗星核として描かれうる状態になっていた)。

天体の描画は `bodyRenderer.beginPass / draw / endPass` を通す。`draw()` は呼ばれるたびに天体単位の uniform を**すべて**設定する — 条件分岐で省かない。

`drawAtmos()` (地球の大気シェル) だけはパスの途中でブレンド・深度書き込み・カリングを変える。**本体をすべて描き終えてから最後に呼ぶ**こと (加算合成なので順序が効く)。変えた状態は `drawAtmos()` 内で `beginPass` の設定へ戻している。

### コンテキストを失ったら開き直す

WebGL のコンテキストは GPU 側の都合で失われる — ドライバのリセット、スリープからの復帰、他のタブとの取り合い、メモリ不足。高解像度テクスチャ (常駐 625MB) を選んだ端末では実際に起きうる。

`gl/setup.js` で `webglcontextlost` を捕まえ、**`preventDefault()`** して (これが無いと `webglcontextrestored` が来ない) 描画ループを止め、案内を出す。復帰の合図が来たら、いまの日時・視点を共有 URL に載せて **開き直す**。

**その場で作り直すことはしない。** プログラム・バッファ・テクスチャの生成が起動時の一度きりで全ファイルに散っており、作り直せる形にするには全面的な組み替えが要る。裏を返すと、GL オブジェクトを増やすときに復帰のことを考えなくてよい。開き直しても画像は Service Worker の控えから来るので、取り直しは発生しない。

開き直しは **1セッションに1回だけ** (`sessionStorage` の `ssGlReloaded`)。原因が続いていると延々と往復するため。2回目からは案内を出したままにして、「読み込み直す」を押すかどうかは利用者に委ねる。あわせて `ssHiRes` を消してから開く — 同じ設定のまま開き直すと同じところで落ちる。

共有 URL にビュー (宇宙/地上/月面) は載っていないので、開き直すと宇宙ビューに戻る。

### オーバーレイの文字は `lblPut` で積む

キャンバスへ文字を出すときは `octx.fillText` を直に呼ばず、`src/render/body.js` の `lblPut(文字, x, y, 優先度, 色, 書体)` へ積む。各ビューのオーバーレイは先頭で `lblBegin()`、末尾で `lblEnd()` を呼び、`lblEnd()` が優先度の高いものから場所を取らせて、確保済みの矩形と重なるものを捨てる。

文字の系統は 天体・衛星・探査機・方位・放射点・星雲・星座 と多く、それぞれが自分の都合で描くと内惑星まわりのように混み合ったところで全部重なって読めなくなる。優先度は `LBL_SEL`(選択・注目天体。必ず出す) < `LBL_BODY` < `LBL_PROBE` < `LBL_SAT` < `LBL_DIR` < `LBL_MET` < `LBL_DSO` < `LBL_SKY`(星座名・黄道) の順。**同じ優先度どうしは積んだ順が勝つ** (`sort` は安定) ので、天体の並びを変えないかぎり毎フレーム同じものが残る — 拮抗した2つが交互に現れて点滅することはない。並び順に乱数や `Map` の反復順を混ぜないこと。

`lblBlock(x, y, r)` は天体の円盤を「文字を置かせない場所」として先に確保する。弾くのは `LBL_MET` 以下の背景側だけで、木星面を通過中の衛星のように円盤の上にあること自体が意味を持つ名前は通す。

リング・破線・放射点の印は重なっても読めるので、これまで通り直に描いてよい。文字の幅は `measureText` を毎フレーム何百回も呼ばないようキャッシュしてある (言語を切り替えても別のキーになる)。

### 全球マップは `SAMPLE()` で取る

`body.frag` で正距円筒のテクスチャを読むときは、`texture2D` を直に呼ばず `SAMPLE(tex, uv)` を使う。経度方向は必ずどこかで折り返すので、微分を自動で取らせるとその1列だけミップ段が最粗まで落ち、天体を縦に切る縞が出る (地表で1回、雲でもう1回やらかしている)。uv を `fract()` で畳むのも同じ理由で禁止 — 折り返しは `REPEAT` に任せる。

### 空は J2000 のまま、観測地を歳差で戻す

天体の位置も恒星カタログも J2000 平均分点。一方、地球の自転はその日のグリニッジ恒星時で表す。この2つをそのまま突き合わせると空全体が一様に回転してずれる (2026年で 0.37°、100年で 1.4° ずつ増える)。星と天体の位置関係は合うので画では気づきにくいが、出没・南中・方位が実際からずれる。

**空の側を回さず、観測地の基底を J2000 へ戻す**こと (`precessFrom`)。恒星8,400個と星座線を毎フレーム回すのに対して、回すのは天頂・北・東の3本で済む。逆に、赤経赤緯を直接扱う計算 (`computeObs` の出没、放射点の高度) では天体の側をその日の分点へ送る (`precessTo`)。どちらも `core/math.js`。

出没の時角の進みは天体ごとに違う (`haRate`)。恒星時の進み 15.041°/時 をそのまま使うと、太陽で約1分、月で最大15分ずれる — 太陽の時角は 15.000°/時 しか進まない (平均太陽日の定義)。

### 大気差は地平フレームへ落とすものすべてに掛ける

出没時刻は h0 = −0.833° (大気差 34′ + 視半径 16′) で解いているので、描画側にも大気差が要る。無いと画面のほうが3分ほど早く沈む。

**天体だけに掛けないこと。** 地平ぎわで星座線と星が最大 34′ (月の直径ぶん) ずれる。地平フレームの単位ベクトルを持ち上げる `refractUp()` を、恒星・星座線・黄道・経緯線・放射点に掛ける。天の川はシェーダ側で、画素の向きから逆に下げてから地図を引く (`sky.frag` の `uRefr`)。式は `core/math.js` の `refractRad()` に1本だけ置く。

`computeObs()` が返す `alt` は**見かけの高度** (大気差込み)。出没の計算はこれを使わず `dec` と時角から真高度のまま解く。月面ビューには大気が無いので `refractUp()` は素通りする。

### 大気減光は「天体から届く光」だけに掛ける

大気差 (`refractRad`) と対になる量として、大気減光 (`extinct` / `EXT_TBL`, `core/math.js`) がある。大気路長は Kasten & Young。**天頂を 1 に正規化**してあるので、これまでの明るさの調整はそのまま使える。係数は RGB 別 (`EXT_K = [0.13, 0.19, 0.28]`) — 青ほど強く減るので、地平ぎわの太陽と月は勝手に赤くなる。

掛ける先は**光**だけ。恒星・点で描く天体・球で描く天体 (`uExt`)・天の川 (`sky.frag` の `uExtK`) に掛ける。星座線・経緯線・黄道・選択マークは光ではなく画面上の注記なので掛けない。

**エアライトには掛けないこと。** `body.frag` の `c *= uExt` は `skyDayColor()` を足す**前**に置く。エアライトは天体と目のあいだの大気そのものが光っているぶんなので、そこへ減光を掛けると二重に減る。

太陽の円盤と大気シェルは早期 return する別の経路なので、そこにも個別に掛けてある。太陽のグレアの明るさと色も、手置きの赤み係数をやめて `extinct()` の透過率そのものから出している — 円盤とグレアが別の法則で赤くなっていると噛み合わない。

**早期 return する経路では、減光とエアライトを対で書くこと。** 太陽の円盤は長らく `tonemap(c * uExt)` だけで、エアライトを足していなかった。地平ぎわでは減光が効いて円盤が暗くなる一方、まわりの空にはエアライトが乗るので、**夕日が空に開いた黒い穴になる**。天体の経路のコメントが「新月ごろの月が青空に黒い円盤として浮いてしまう」と書いているのとまったく同じ話で、太陽だけが取り残されていた。正しくは `tonemap(c * uExt + skyDayColor(...) * uAirGain)`。大気が無い経路 (宇宙ビュー・月面) では `skyDayColor` が `uAirFlux <= 0.0005` で 0 を返すので、条件分岐は要らない。

月面ビューには大気が無いので、`refractUp()` と同じく素通り (`ext: null`)。

### 空の明るさは散乱の積分から取る

空の色は `src/shaders/sky-color.glsl` の散乱の積分で出している。星の見え方 (`starVis`)・Bloom のしきい値・天体に乗せるエアライトも、**同じ積分から取った天頂の明るさ**で決める。太陽高度の一次式に戻さないこと — それをやっていたときは薄明が市民薄明 (−6.9°) で打ち切られ、東京で1時間ほど早く真っ暗になっていた。

- 定数 (`ATM_*`) は `src/core/atmos.js` に1箇所だけ置き、GLSL へは `ATM_GLSL` として前置きする。JS 側は同じ積分を**天頂についてだけ**解いた `skyZenithLum()` を持つ (毎フレーム1回)
- 単散乱だけでは、視線上の大気がすべて地球の影に入った時点で薄明が終わる。本影へにじむ多重散乱 (`ATM_SHADOW` / `ATM_LEAK`) を足して −13.5° まで延ばしてある
- 実際の薄明は5桁ぶん暗くなるが画面は2桁ほどしか出せないので、`skyAdaptGain()` で暗いところほど感度を上げて描く (目の順応)。生の輝度をそのまま出すと、市民薄明のうちに真っ黒になる
- 日食の暗転は `flux`(= 隠れずに残っている太陽面の割合) を積分に掛けて出す。単散乱は入射光量に比例するので、これが物理的に正しい掛け方。皆既でも 0 にはしない — 本影の帯は幅 100km ほどで、まわりの陽の当たった大気からの散乱が入る
- 地面の明るさ (`uDay`) だけは直射日光で決まるので、こちらは太陽高度の式のまま

### 星雲・星団は面輝度で描く

`src/render/dso.js` は、等級から出した光の量を**見かけの面積で割った面輝度**を板の中心の明るさにする。板の大きさは天球上で固定なので、寄っても画面上の明るさは変わらない — 実際の面輝度と同じふるまいで、M31 のように大きくて淡いものは寄るほど薄く広がる。「等級が明るいほど明るく描く」にしてはいけない (M31 は 3.4等だが、広がっているので実際には淡い)。

**散開星団だけは弱める** (`DSO_OCL`)。明るい星は恒星カタログ (6.5等まで) が既に点で描いているので、そこへ全光量ぶんの光を重ねると二重になる。残りはカタログに入らない暗い星のぶん。

データ (`src/data/dso.js`) は OpenNGC 由来で **CC-BY-SA-4.0**。コード本体の MIT とは条件が違うので、差し替え・再配布のときは出典表示ごと扱うこと。継承条件がかかるとしても対象はこのデータファイルであって、リポジトリ全体のライセンスが変わるわけではない。

**PD / CC0 の出典に替えられないか、2026-08-27 に調べた結果** (同じ調査を繰り返さないための記録):

| 項目 | Wikidata (CC0) | NASA の Messier ページ (PD) | OpenNGC (CC-BY-SA) |
|---|:---:|:---:|:---:|
| 位置・視等級・種別 | ○ | 一部 | ○ |
| 和名・多言語名 | ○ | ✗ | ✗ |
| **視直径 (長径・短径・位置角)** | **✗** | **✗** | **○** |

視直径だけはどちらにも無い (Wikidata は M31 のプロパティ77個を全部見て確認、NASA のページにも "Apparent Dimension" は無い)。面輝度は「光の量 ÷ 見かけの面積」なので、視直径を落とすと描き方そのものが成り立たない。

数値は事実なので著作権は及ばず、「メシエ天体110個」も歴史的に決まった集合で創作的な選択ではない。日本には EU のようなデータベース権も無い。**それでも出典表示は他のデータ (Yale BSC・d3-celestial・NASA SVS) と同じく付ける方針** — コストがクレジット1行ぶんしかないため。取る量を減らしたくなったら、位置・等級・名前を Wikidata (CC0) から取り直し、OpenNGC からは視直径3項目だけにする手がある (和名が109個すべてに付くおまけもある)。

### Bloom は画面を取り込んでから

`src/gl/post.js` の `bloomPass()` は、シーンを描き終えた既定のフレームバッファを `copyTexSubImage2D` で取り込み、明るいところをぼかして加算で戻す。**オフスクリーンへ描く方式にしないこと** — canvas の `antialias: true` (MSAA) が効かなくなり、軌道線と天体の輪郭がギザギザになる。

取り込み先のテクスチャは `gl.RGB`。canvas が `alpha: false` なので既定のフレームバッファにアルファが無く、`gl.RGBA` へコピーすると `INVALID_OPERATION` になる。

### 色はリニアで計算する

`src/gl/setup.js` の `PRE` に `srgbToLinear` / `linearToSrgb` / `acesToneMap` / `tonemap` がある。照明を計算するシェーダは次の順で書く。

```
sRGB のテクスチャ・定数 → リニア → 照明・大気・発光を加算 → tonemap() → gl_FragColor
```

- **アルベド** (テクスチャ、天体の色) は `srgbToLinear`。天体の `colA/colB/colC/rim` は `bodies.js` で変換済みの `colAL/colBL/colCL/rimL` を渡す (画素ごとに `pow` を回さないため)。シェーダ内の即値も、リニア値を直に書いてコメントに元の sRGB を残す
- **見た目を決め打ちした色** (空の色、霞、地球照) は「`tonemap` を通すと狙った画面色になる」逆算値を書く。EXPOSURE を変えたら計算し直す
- **透過率・光学的厚さ・マスク**はもともとリニアなので変換しない (環プロファイルの `.a` など)
- 加算合成だけの発光パス (`point` / `bill` / `coma` / `tail` / `line`) は画面色を直に書いており、トーンマップを通していない

### 光源のパスは「アルファ 0 の乗算済みアルファ」で足す

ブレンドは `ONE, ONE_MINUS_SRC_ALPHA` を共有しているので、**フラグメントが返すアルファがそのまま「加算か、置き換えか」を決める**。

- `gl_FragColor = vec4(col, 0.0)` … 背景へ**足す**。光源 (恒星・点で描く天体・グレア・尾) はこちら
- `gl_FragColor = vec4(col * a, a)` … 背景を**置き換える**。不透明な面はこちら

点で描く天体は長らく後者だった。夜空 (背景がほぼ 0) では両者の値が一致するので誰も気づかないが、**明るい背景の上では暗い光源が穴を開ける** — 夕空の太陽が黒い円盤になっていたのがこれ。地平ぎわの太陽は大気減光で 0.16 倍ほどまで落ちるので、明るい薄明の空に対しては「暗い光源」になる。

同じ天体でも、画面上の大きさで球 (`body.frag`) と点 (`point.frag`) を行き来する (`spherePx >= 5`)。**片方だけ直しても、ズームを変えると再発する。**

## その他

- **絵だけのボタンは `ARIA_BTNS` に積む** (`src/ui/observe.js`)。読み上げ名は天体名を含み言語で変わるので、名前の作り方 (`() => T().ariaOrbit(bName(b))` のような関数) をボタンと一緒に控えて、`applyLang` が `refreshNavAria()` で入れ直す。積み忘れると読み上げが「ボタン」だけになる。ON/OFF は色ではなく `aria-pressed` で伝えるので、状態を変えたら `syncToggleUI()` を通すこと (ツアーが `b.showOrbit` を直接いじって一覧と食い違っていた)
- **`#clock` は `pointer-events: none`** (時計まわりの余白でもカメラを回せるように)。中に押せるものを足したら、その要素に `pointer-events: auto` を戻すこと。忘れるとクリックが背後のキャンバスへ抜け、押しても無反応どころか天体の選択が外れる (時刻の基準の切替ボタンが実際そうなっていた)
- コミットメッセージは日本語。何をなぜ変えたかを本文に書く
- `docs/` は未追跡のまま (コミットしない)
